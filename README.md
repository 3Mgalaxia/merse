# Merse 🚀

Plataforma cósmica de IA que transforma fotos, gera imagens realistas e entrega experiências imersivas para a galáxia Merse.

## Requisitos

- Node.js 18+
- pnpm, npm, yarn ou bun

## Como rodar

```bash
npm install
npm run dev
# ou use yarn / pnpm / bun conforme preferir
```

Abra <http://localhost:3000> para navegar.

## Estrutura

- `pages/` — rotas Next.js e APIs internas
- `components/` — componentes UI Merse
- `contexts/` — provedores globais (auth, energia cósmica etc.)
- `public/` — banners e assets oficiais

## Deploy

Recomendado usar [Vercel](https://vercel.com/) ou ambiente compatível com Next.js.

---

Mantenha o `.env.local` fora do controle de versão e ajuste as integrações (OpenAI, Replicate, Firebase) antes de publicar.💫
